name: W4

on: 
  schedule:
    - cron: "0 8 * * 0-6"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v7
      - name: staeting msg
        run: echo Job has started.
      - name: current date and time
        run: date
      - name: completion
        run: echo Task has been completed.
